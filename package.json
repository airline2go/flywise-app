{
  "name": "airpiv-app",
  "version": "1.0.0",
  "private": true,
  "description": "Airpiv static frontend (no build step — served as plain HTML/CSS/JS)",
  "scripts": {
    "format": "prettier --write \"*.css\""
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  }
}
