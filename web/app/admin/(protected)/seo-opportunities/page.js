// Server Component shell — the SEO opportunity report is interactive
// (loading, column sorting, category filtering), so it all lives in the Client
// Component below.
import SeoOpportunitiesClient from './SeoOpportunitiesClient';

export default function SeoOpportunitiesPage() {
  return <SeoOpportunitiesClient />;
}
