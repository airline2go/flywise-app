{
  "name": "airpiv-visual-parity",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Pixel-by-pixel visual-regression harness that proves the Next.js migration renders identically to the legacy HTML/CSS/JS frontend. Captures screenshots of both sites at mobile/tablet/desktop in every language and diffs them; any non-zero difference is a regression.",
  "scripts": {
    "baseline": "node capture.mjs --target=legacy",
    "candidate": "node capture.mjs --target=candidate",
    "compare": "node compare.mjs",
    "check": "node run.mjs"
  },
  "dependencies": {
    "pixelmatch": "^6.0.0",
    "playwright": "1.56.1",
    "pngjs": "^7.0.0"
  }
}
